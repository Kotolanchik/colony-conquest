using Unity.Entities;

namespace ColonyConquest.Core
{
    /// <summary>
    /// Маркер сущностей, участвующих в синтетической нагрузке (дрейф позиции).
    /// </summary>
    public struct BenchmarkDriftTag : IComponentData
    {
    }
}
